import axios from "axios";
import { defineStore } from "pinia";
import { api_url } from "@/config";

export const useProfileDataStore = defineStore("ui_data", {
  state() {
    return {};
  },
  getters: {},
  actions: {},
});
